<h1>Flats</h1>

<ul>
  <% @flats.each do |flat| %>
  <li>
    <h2><%= flat['name'] %></h2>
    <p>💰 <%= flat['price'] %> <%= flat['priceCurrency'] %></p>
    <img src="<%= flat['imageUrl'] %>" alt="flat image" width='200px'>
  </li>
  <% end %>
</ul>
