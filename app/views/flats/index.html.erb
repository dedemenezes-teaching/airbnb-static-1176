<h1>Flats</h1>

<ul>
  <% @flats.each do |flat| %>
  <li>
    <div class="card" style="width: 18rem;">
      <img src="<%= flat['imageUrl'] %>" class="card-img-top" alt="flat image" width='200px'>
      <div class="card-body">
        <h2 class="card-title"><%= link_to(flat['name'], flat_path(flat['id'])) %></h2>
        <p class="card-text">💰 <%= flat['price'] %> <%= flat['priceCurrency'] %></p>
      </div>
    </div>
    <%# <a href="/flats/???"></a> %>
  </li>
  <% end %>
</ul>
